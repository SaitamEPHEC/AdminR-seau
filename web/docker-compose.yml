version: '3.1'
services:
 letsencrypt-nginx-container:
  container_name: 'webcrypted'
  image: nginx:latest
  ports:
   - "80:80"
  volumes:
   - ./nginx.conf:/etc/nginx/conf.d/default.conf
   - ./letsencrypt-site:/usr/share/nginx/html
   - ./index.html:/usr/share/nginx/html/index.html
